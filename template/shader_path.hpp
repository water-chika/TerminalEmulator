#pragma once
#include <filesystem>

inline std::string vertex_path = "${vertex_path}";
inline std::string fragment_path = "${fragment_path}";
inline std::string mesh_path = "${mesh_path}";
inline std::string geometry_path = "${geometry_path}";